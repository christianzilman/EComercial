@model IEnumerable<EComercial.Models.FormaEntrega>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoEntrega.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Costo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pedido.PedidoId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoEntrega.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Costo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pedido.PedidoId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FormaEntregaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FormaEntregaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FormaEntregaId })
        </td>
    </tr>
}

</table>
