@model EComercial.Models.Item

@{
    ViewBag.Title = "Eliminar";
}

<div class="panel panel-danger">
    <div class="panel-heading">
        <h2 class="panel-title">Items - ¿Está seguro de que desea eliminar esto?</h2>
    </div>
    <div class="panel-body">
        <div class="col-md-3">
            <img src="~/Images/e.png" width="80" height="80" />
            <div>    Nombre: @Html.DisplayFor(model => model.Nombre) </div>
            <div>    SubCategoria: @Html.DisplayFor(model => model.SubCategoria.Nombre)</div>
            <br />
            <div>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div>
        <input type="submit" class="btn-primary" value="Eliminar" /> </div>
    <div>
        @Html.ActionLink("Volver al listado", "Index")
    </div>
    }

</div>
        </div>
    </div>
</div>

